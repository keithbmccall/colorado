import { StyleSheet } from "react-native";
import { DEVICE_WIDTH } from "#styles";
//

export default StyleSheet.create({
  imageContainer: {
    width: DEVICE_WIDTH,
    resizeMode: "contain"
  },
  colorStripWrapper: {
    height: "100%"
  }
});
